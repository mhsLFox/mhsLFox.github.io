

function setup() {
  // put setup code here
  createCanvas(1200,877.49);

  /the square
}
noStroke();
draw();
 function draw() {
    rect(mouseX, mouseY, 50, 50);
   
//when mouse is clicked square changes colors
};
function mouseClicked() {
  fill(random(255),random(255),random(255));
}
